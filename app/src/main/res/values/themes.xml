<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.HashScanner" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.HashScanner" parent="Base.Theme.HashScanner" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#3D5AFE</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/shild</item>
        <item name="postSplashScreenTheme">@style/Theme.HashScanner</item>
    </style>
</resources>